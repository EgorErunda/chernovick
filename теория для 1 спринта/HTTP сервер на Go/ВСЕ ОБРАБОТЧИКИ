Handler — это интерфейс, который нужно реализовать для создания обработчика.

HandlerFunc — это тип функции, который удовлетворяет интерфейсу Handler.

Handle — это функция для регистрации обработчика, реализующего интерфейс Handler.

HandleFunc — это упрощённая функция для регистрации простой функции в качестве обработчика.